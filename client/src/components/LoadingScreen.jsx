import { FaExclamationTriangle } from "react-icons/fa";
export default function LoadingScreen({title="Preparando la partida…",detail="Conectando con la sala",error,onBack}){return <main className="game-loading" aria-live="polite" aria-busy={!error}><section className="game-loading__card">{error?<FaExclamationTriangle className="game-loading__error" aria-hidden="true"/>:<div className="game-loading__spinner" aria-hidden="true"><span/></div>}<p className="game-loading__brand">100 Mexicanos Dijeron</p><h1>{error||title}</h1><p>{error?"Revisa el código de sala o la conexión e inténtalo nuevamente.":detail}</p>{error&&onBack&&<button type="button" className="btn btn-warning mt-6 w-full font-black" onClick={onBack}>Volver al inicio</button>}</section></main>}
